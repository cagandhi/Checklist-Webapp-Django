language: python
python:
  - "3.7"
# command to install dependencies
# env:
#   DB=sqlite
install:
  - python setup.py install
  - pip install coverage
  - pip install codecov
# command to run tests
git:
  depth: 50
jobs:
  include:
  - name: "django-project"
    python: "3.7"
script:
  - python manage.py test
  # - coverage run --source=. checklist_project/manage.py test --settings=checklist_project.settings
  # - coverage run --source="checklist" setup.py test
  # - coverage run --source='.' manage.py test checklist
  # - coverage run manage.py test
after_success:
  - bash <(curl -s https://codecov.io/bash) -t 0595e9dc-f36d-439a-85bf-0e27891785a9
